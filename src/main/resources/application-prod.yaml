spring:
  application:
    name: GOFV
  datasource:
    url: jdbc:postgresql://localhost:5432/gate_of_heaven
    username: kiran_kumar
    password: Tkkcode@09032707
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    user:
      name: user
      password: user
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: 8080
aws:
  region: ap-southeast-2
  s3:
    bucket-name: my-personal-web-bucket-987989462612-ap-southeast-2-an
kind: Application